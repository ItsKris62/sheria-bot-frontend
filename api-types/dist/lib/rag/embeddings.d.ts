/**
 * Embedding provider type
 * Using Pinecone integrated embeddings only.
 */
export type EmbeddingProvider = 'pinecone-integrated';
/**
 * Embedding configuration
 */
interface EmbeddingConfig {
    provider: EmbeddingProvider;
    model: string;
    dimension: number | null;
}
/**
 * Get embedding configuration
 */
export declare function getEmbeddingConfig(): EmbeddingConfig;
/**
 * IMPORTANT:
 * In integrated mode, embeddings are generated by Pinecone
 * during upsert. These functions should NEVER be called.
 */
export declare function generateEmbedding(): Promise<never>;
export declare function generateBatchEmbeddings(): Promise<never>;
/**
 * Cosine similarity utility (useful for testing / diagnostics)
 */
export declare function cosineSimilarity(a: number[], b: number[]): number;
/**
 * Normalize vector (optional utility)
 */
export declare function normalizeVector(vector: number[]): number[];
/**
 * Embedding stats (static in integrated mode)
 */
export declare function getEmbeddingStats(): Promise<{
    provider: EmbeddingProvider;
    model: string;
    dimension: number | null;
    cacheSize: number;
}>;
/**
 * Clear embedding cache (no-op in integrated mode)
 */
export declare function clearEmbeddingCache(): Promise<number>;
export {};
//# sourceMappingURL=embeddings.d.ts.map